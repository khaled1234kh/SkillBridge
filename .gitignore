# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Database (re-seeded on boot)
*.db

# Frontend
frontend/node_modules/
frontend/dist/
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.vscode/

# Local handoff / scratch docs (not part of the product)
AGENT_HANDOFF.md
Installing

# Docker experiments (not the validated working path)
Dockerfile
.devcontainer/
devcontainer-lock.json

# Nested duplicate checkout (never commit)
SkillBridge/